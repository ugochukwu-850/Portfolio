{
  "name": "chizaram-portfolio",
  "private": true,
  "scripts": {
    "css:build": "tailwindcss -i input.css -o static/style.css --minify",
    "css:watch": "tailwindcss -i input.css -o static/style.css --watch",
    "build": "npm run css:build && zola build",
    "serve": "zola serve"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
